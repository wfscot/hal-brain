---
description: "Always ask before destructive actions — deletes, force pushes, production deploys"
globs: ["**/*"]
---

# Ask Before Destructive Actions

Deletes, force pushes, production deploys — always ask first with context about consequences. Check git remote before push. Don't modify user content without asking. "Create a plan" means present and stop — no execution without approval.
