#!/usr/bin/env bash
set -euo pipefail

# brain-publish — Push to remote.
# Usage: brain-publish

REPO_DIR="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"
cd "${REPO_DIR}"
git push
